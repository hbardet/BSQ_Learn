/*
** EPITECH PROJECT, 2022
** my_is_lower
** File description:
** return if char is alpha or not
*/

int my_is_lower(char c)
{
    if (c >= 'a' && c <= 'z'){
        return (1);
    }
    return (0);
}
